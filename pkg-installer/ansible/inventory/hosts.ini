# 대상 호스트 인벤토리 (폐쇄망 내부). 실제 값은 환경에 맞게 채운다.
# OS 그룹으로 분기(group_vars/centos7.yml, rhel8.yml).

[centos7]
# node-c7-01 ansible_host=10.0.0.11 ansible_user=root

[rhel8]
# node-r8-01 ansible_host=10.0.0.21 ansible_user=root

[all:vars]
ansible_connection=ssh
